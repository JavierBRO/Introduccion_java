package com.certidevs.funciones2;

public class CalculadoraAvanzada {
   // invocar funciones


    public static double calcularMedia(double[] numbers) {
        for (int i = 0; i < numbers.length; i++) {
        }
        return (numbers[0] + numbers[1] + numbers[2]) / numbers.length;

    }

}
